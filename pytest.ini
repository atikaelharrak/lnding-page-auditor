[pytest]
markers =
    network: tests that mock or exercise network-related code paths
    integration: tests that make real network requests to live sites (slower, requires internet)
testpaths = tests
python_files = test_*.py
